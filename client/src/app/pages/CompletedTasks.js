const CompletedTasks = () => {
  return <div>CompletedTasks</div>;
};

export default CompletedTasks;

// modify allTasks function to send either is_completed, is_archived or is_deleted tasks
// based on data received from the client.
